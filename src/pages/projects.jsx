import React from 'react';
import PropTypes from 'prop-types';

const Projects = props => {
	const { data } = props;
	console.log(data);
	return (
		<div className="swas-projects">
			<h5>Something Projects</h5>
		</div>
	);
};

export default Projects;
